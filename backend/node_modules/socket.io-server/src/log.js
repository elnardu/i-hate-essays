module.exports = function(name, format, ...args)
{
    format =
            '[' +
            Date.now().toString(16) +
            '][' +
            name.split('.').join('][') +
            ']: ' +
            format;
    
    args.unshift(format);
    
    console.log.apply(console, args);
};
